# Copy this to .env and fill in your keys
# .env is gitignored — never commit real keys

# Used by the CLI (--api-key flag also works)
JUDGE_API_KEY=

# litellm picks these up automatically by provider
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
